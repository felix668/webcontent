import '../js/rem.js';
Vue.component( 'app',require('../../src/app.vue') );
	var root = new Vue({
		el: '#root',
		template: '<app></app>'
	});
document.body.addEventListener('touchstart', function () {
 //...空函数即可
});  

